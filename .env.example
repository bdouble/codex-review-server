# Codex Delegation MCP Server Configuration
# Copy to .env and adjust as needed. All values shown are defaults.
#
# The older CODEX_REVIEW_* names are still honoured as a fallback, so an
# existing .env keeps working. New installs should use the CODEX_* names below.

# Codex CLI home directory (where config.toml and auth credentials live)
# CODEX_HOME_DIR=~/.codex

# Model. Run the codex_models tool for the live catalog on your account —
# it is the authority, and it lists models released after this file was written.
#   gpt-6-astra              — frontier; the most demanding work
#   gpt-6-sol                — workhorse for coding and everyday work (default)
#   gpt-6-luna               — fast and affordable; easier tasks
#   gpt-daybreak-blue-latest — defensive cybersecurity work (access-gated)
#   gpt-5.6-sol / -terra / -luna, gpt-5.5 — previous generations
CODEX_MODEL=gpt-6-sol

# On an account approved for Daybreak Blue, a model with a Daybreak build runs
# as that build: gpt-5.6-sol becomes gpt-daybreak-blue-latest (the same
# snapshot under the Daybreak cyber program). gpt-6-sol has no Daybreak build
# reachable through `codex exec`, so it is unaffected. Set false to disable.
# CODEX_PREFER_DAYBREAK=true

# Reasoning effort: low, medium, high, xhigh, max, ultra
# Support is per-model and the catalog decides. Astra, Sol, Terra and Daybreak
# go all the way to `ultra`; Luna stops at `max`; 5.5 stops at `xhigh`.
# `ultra` coordinates several agents in parallel and is substantially slower.
CODEX_EFFORT=high

# Timeout per call in seconds (repo-aware work is thorough; ultra runs longer)
CODEX_TIMEOUT=4500

# Review focus: bugs, security, performance, all
CODEX_FOCUS=all

# Where job records live
# CODEX_STATE_DIR=~/.codex-review-server

# Max job records retained before the oldest are pruned
# CODEX_MAX_JOBS=50
